# Retry Path Flow

This flow is used to validate retry behavior.

Execution outline:

- Start from the flow summary.
- Call `next` to enter the first step.
- Continue through the retry-oriented steps and confirm that predefined retry steps behave correctly.
