# Finish can_next Test

Complete the dedicated `can_next` validation flow.

Before calling `workflow.next`, confirm that context contains:

- `ready`
- `token`

Rules:

- This step exists only to finish the `can_next` test flow.
