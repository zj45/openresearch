# Capture Value Without Explicit Persistence Instruction

Assume the current step has received this value:

```json
{
  "net_can": "implicit_capture_value"
}
```

Required actions:

- Finish this step and continue the workflow.
